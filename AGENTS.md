# Audio Label Pro - AI Agent 上下文文档

> **项目名称**: Audio Label Pro  
> **项目类型**: 专业语音数据标注平台 (SaaS)  
> **最后更新**: 2026-02-14  
> **当前状态**: 开发中（部分功能已实现）

---

## 📋 项目概述

**Audio Label Pro** 是一个基于 Web 的语音数据标注平台，支持大规模语音数据的标注、审核和管理工作，为机器学习模型训练提供高质量标注数据。

### 核心功能

- **音频管理**: 支持批量上传、预览、文件夹管理
- **音频播放器**: 波形显示、变速播放、循环播放、快捷键控制
- **标注功能**: 时间区间选择、多层标注、文本转写、标签体系
- **AI 辅助**: 自动语音识别（ASR）、说话人分离、自动分段、智能标签推荐
- **热力图模块**: 标注密度、说话人分布、频谱热力图
- **实时协作**: 多人协作编辑、编辑锁定、协作者光标、评论系统
- **质量控制**: 审核流程、问题标记、质量评分
- **任务管理**: 任务创建、分配、进度跟踪、统计报表
- **数据导出**: 支持 JSON、CSV、TXT、SRT、VTT、XML 格式

### 设计理念

- **风格**: Minimalism & Swiss Style（极简主义 + 瑞士风格）
- **关键词**: 简洁、专业、高效、可信
- **目标用户**: B2B 企业、标注团队、AI 研究人员

---

## 🏗️ 项目架构

### 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                           客户端层 (Web)                             │
│  Vue 3 + TypeScript + Vite + WaveSurfer.js + Socket.io Client      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼ HTTPS/WSS
┌─────────────────────────────────────────────────────────────────────┐
│                           负载均衡层 (Nginx)                         │
│                    SSL 终止、静态资源、反向代理                       │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                ┌───────────────────┼───────────────────┐
                ▼                   ▼                   ▼
┌───────────────────────┐ ┌───────────────────────┐ ┌───────────────────────┐
│    后端服务集群        │ │   AI 服务集群          │ │   WebSocket 集群      │
│   NestJS + Express    │ │   Python + FastAPI    │ │   Socket.io           │
│   (多实例)            │ │   (多实例)            │ │   (Redis Adapter)     │
└───────────────────────┘ └───────────────────────┘ └───────────────────────┘
                │                   │                   │
                └───────────────────┼───────────────────┘
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           数据层                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │ PostgreSQL  │  │    Redis    │  │  MinIO / S3 │  │  Bull 队列   │ │
│  │  主数据      │  │  缓存/锁     │  │  音频存储    │  │  任务队列    │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 目录结构

```
audio-label/
├── frontend/              # Vue 3 前端应用
│   ├── src/
│   │   ├── assets/        # 静态资源
│   │   │   └── styles/    # 样式文件
│   │   ├── pages/         # 页面组件
│   │   │   ├── HomePage.vue
│   │   │   ├── LoginPage.vue
│   │   │   ├── RegisterPage.vue
│   │   │   ├── AudioListPage.vue
│   │   │   ├── AnnotationPage.vue
│   │   │   ├── ReviewPage.vue
│   │   │   └── TaskListPage.vue
│   │   ├── router/        # 路由配置
│   │   ├── services/      # API 服务
│   │   ├── stores/        # Pinia 状态管理
│   │   └── types/         # TypeScript 类型定义
│   ├── package.json
│   └── vite.config.ts
│
├── backend/               # NestJS 后端服务
│   ├── src/
│   │   ├── auth/          # 认证模块 ✅ 已实现
│   │   ├── users/         # 用户模块 ✅ 已实现
│   │   ├── projects/      # 项目模块 ✅ 已实现
│   │   ├── teams/         # 团队模块 ✅ 已实现
│   │   ├── audio/         # 音频模块 ⏳ 待实现
│   │   ├── annotations/   # 标注模块 ⏳ 待实现
│   │   ├── ai/            # AI 模块 ⏳ 待实现
│   │   ├── collaboration/ # 协作模块 ⏳ 待实现
│   │   ├── tasks/         # 任务模块 ⏳ 待实现
│   │   ├── export/        # 导出模块 ⏳ 待实现
│   │   ├── notifications/ # 通知模块 ⏳ 待实现
│   │   ├── database/      # 数据库模块 ✅ 已实现
│   │   ├── app.module.ts
│   │   └── main.ts
│   ├── package.json
│   └── .env.example
│
├── ai-service/            # FastAPI AI 服务
│   ├── app/
│   │   ├── api/
│   │   │   └── endpoints/ # API 端点
│   │   │       ├── asr.py
│   │   │       ├── speaker_diarization.py
│   │   │       └── segmentation.py
│   │   ├── core/          # 核心配置
│   │   └── main.py
│   ├── requirements.txt
│   └── .env.example
│
├── docker/                # Docker 配置
│   └── postgres/
│       └── init.sql
│
├── docs/                  # 项目文档
│   ├── design-system.json
│   ├── design-system.md
│   ├── requirements.md
│   ├── technical-specification.md
│   ├── DEVELOPMENT_PLAN.md
│   ├── PROJECT_STRUCTURE.md
│   ├── component-design/
│   ├── design-guidelines/
│   ├── html-prototype/
│   ├── prd/
│   └── prototype/
│
├── docker-compose.yml
├── package.json
├── README.md
└── QUICKSTART.md
```

---

## 🛠️ 技术栈

### 前端技术栈

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| Vue 3 | 3.5.28 | 核心框架 | Composition API、响应式系统 |
| TypeScript | 5.9.3 | 类型系统 | 类型安全、开发体验好 |
| Vite | 7.3.1 | 构建工具 | 极速热更新、原生 ESM |
| Pinia | 3.0.4 | 状态管理 | Vue 3 官方推荐、轻量 |
| Pinia Persistedstate | 4.7.1 | 状态持久化 | 自动持久化 Pinia 状态 |
| Vue Router | 5.0.2 | 路由管理 | 官方路由方案 |
| Element Plus | 2.13.2 | UI 组件库 | 企业级组件、中文友好 |
| Tailwind CSS | 4.1.18 | CSS 框架 | 原子化 CSS、快速开发 |
| WaveSurfer.js | 7.12.1 | 波形可视化 | 音频波形渲染 |
| ECharts | 6.0.0 | 图表可视化 | 热力图、统计图表 |
| Axios | 1.13.5 | HTTP 客户端 | 请求拦截、错误处理 |
| Socket.io Client | 4.8.3 | 实时通信 | WebSocket 封装 |
| TanStack Vue Query | 5.92.9 | 数据请求 | 缓存、状态同步 |
| VueUse | 14.2.1 | 工具库 | 组合式工具函数库 |
| Day.js | 1.11.19 | 日期处理 | 轻量级日期库 |
| lru-cache | 11.2.6 | LRU 缓存 | 前端缓存管理 |

### 后端技术栈

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| Node.js | 20+ | 运行时 | LTS 版本、性能稳定 |
| NestJS | 11.0.1 | 应用框架 | 模块化架构、依赖注入 |
| Express | 4.x | HTTP 服务器 | 当前使用的服务器 |
| Fastify | 11.1.13 | HTTP 服务器 | 已安装，待启用 |
| TypeORM | 0.3.28 | ORM | TypeScript 友好 |
| PostgreSQL | 16+ | 关系数据库 | JSON 支持、全文搜索 |
| Redis (ioredis/redis) | 5.9.3/5.10.0 | 缓存/队列 | 高性能、支持数据结构 |
| Socket.io | 4.8.3 | 实时通信 | 房间管理、自动重连 |
| Bull | 4.16.5 | 任务队列 | 基于 Redis 的队列系统 |
| MinIO | 8.0.6 | 对象存储 | S3 兼容、自托管 |
| JWT | - | 认证 | 无状态认证 |
| @nestjs/jwt | 11.0.2 | JWT 认证 | NestJS JWT 模块 |
| Passport | 0.7.0 | 认证中间件 | 认证策略可插拔 |
| Passport JWT | 4.0.1 | JWT 策略 | Passport JWT 策略 |
| Class Validator | 0.14.3 | 参数验证 | 装饰器式验证 |
| Winston | 3.19.0 | 日志管理 | 结构化日志、多传输 |
| audio-decode | 2.2.3 | 音频解码 | Node.js 端音频解码 |
| pako | 2.1.0 | 数据压缩 | 压缩库 |
| bcrypt | 6.0.0 | 密码加密 | 密码哈希 |
| @nestjs/platform-socket.io | 11.1.13 | Socket.io 平台 | NestJS Socket.io 集成 |
| @nestjs/schedule | 6.1.1 | 定时任务 | 定时任务调度 |
| @nestjs/bull | 11.0.4 | Bull 集成 | NestJS Bull 模块 |

### AI 服务技术栈

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| Python | 3.11+ | 主语言 | 主开发语言 |
| FastAPI | 0.109.0 | Web 框架 | 现代、快速的 Web 框架 |
| Uvicorn | 0.27.0 | ASGI 服务器 | 异步服务器 |
| PyTorch | 2.1.0 | 深度学习框架 | 深度学习框架 |
| Whisper (OpenAI) | 20231117 | ASR 语音识别 | 自动语音识别 |
| Pyannote.audio | 3.1.0 | 说话人分离 | 说话人分离 |
| Librosa | 0.10.0 | 音频处理 | 音频分析库 |
| SoundFile | 0.12.0 | 音频 I/O | 音频文件读写 |
| NumPy | 1.26.0 | 数值计算 | 数值计算 |
| Pandas | 2.1.0 | 数据处理 | 数据处理库 |
| Celery | 5.3.0 | 异步任务队列 | 分布式任务队列 |
| Redis | 5.0.0 | 消息代理 | 消息代理 |
| Pydantic | 2.5.0 | 数据验证 | 数据验证 |
| Pydantic Settings | 2.1.0 | 配置管理 | Pydantic 配置管理 |
| MinIO | 7.2.0 | 对象存储客户端 | S3 兼容存储客户端 |
| HTTPX | 0.26.0 | HTTP 客户端 | 异步 HTTP 客户端 |
| AIOFiles | 23.2.0 | 异步文件 I/O | 异步文件操作 |
| Python JSON Logger | 2.0.7 | 日志格式化 | JSON 格式日志 |
| Prometheus Client | 0.19.0 | 监控指标 | Prometheus 指标收集 |
| Python Dotenv | 1.0.0 | 环境变量 | 环境变量加载 |

### 开发工具

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| Node.js | >=20.0.0 | JavaScript 运行时 | LTS 版本 |
| npm | >=10.0.0 | 包管理器 | Node.js 包管理器 |
| concurrently | 8.2.0 | 进程管理 | 并行运行多个命令 |
| rimraf | 5.0.0 | 文件删除 | 跨平台文件删除 |

---

## 🎨 设计系统

### 颜色系统

| 色彩 | 主色 | 辅助色 | 强调色 | 背景色 | 文本色 |
|------|------|--------|--------|--------|--------|
| 色值 | `#059669` | `#10B981` | `#F97316` | `#ECFDF5` | `#064E3B` |
| 说明 | 成功绿 | 浅绿 | 紧急橙 | 浅绿背景 | 深绿文本 |

### 字体系统

- **主要字体**: Poppins
  - 风格: 现代、专业、清晰
  - 适用: 标题、正文、UI 文本
- **次要字体**: Open Sans
  - 风格: 友好、易读
  - 适用: 长文本、说明文字

### 间距系统

- 基础间距: 4px
- 间距比例: 0, 4, 8, 12, 16, 20, 24, 32, 40, 48, 64, 80, 96, 128px

### 设计原则

1. **专业性**: 设计简洁、高效、专业
2. **可信度**: 使用稳定的颜色和清晰的层次
3. **高效性**: 优化用户工作流程，减少操作步骤
4. **可访问性**: 符合 WCAG AAA 标准

### 反设计模式（Anti-patterns）

- ❌ 过度使用动画
- ❌ 信息过载
- ❌ 不一致的交互
- ❌ 缺乏反馈
- ❌ 低对比度
- ❌ 过度装饰

---

## 🗄️ 数据库设计

### 核心表结构

#### 用户相关

- `users` - 用户表
- `teams` - 团队表
- `team_members` - 团队成员表

#### 项目相关

- `projects` - 项目表
- `audio_files` - 音频文件表
- `audio_folders` - 音频文件夹表

#### 标注相关

- `annotations` - 标注表
- `annotation_layers` - 标注层表
- `speakers` - 说话人表
- `tags` - 标签表
- `annotation_tags` - 标注标签关联表
- `comments` - 评论表
- `annotation_versions` - 版本历史表

#### 任务相关

- `tasks` - 任务表

#### 通知相关

- `notifications` - 通知表

#### AI 相关

- `ai_jobs` - AI 处理任务表
- `ai_job_progress` - AI 任务进度表
- `ai_result_chunks` - AI 结果分片表
- `audio_segments` - 音频分段表
- `segment_navigation` - 分段导航目录表
- `segment_bookmarks` - 分段书签表

#### 性能优化相关

- `waveform_cache` - 波形缓存表
- `sessions` - 会话表

### 数据生命周期管理

| 数据类型 | 保留期限 | 清理频率 |
|---------|----------|----------|
| 会话数据 | 30天 | 每日 |
| 通知数据 | 90天 | 每周 |
| AI 结果分片 | 30天 | 每日 |
| AI 任务进度 | 7天 | 每日 |
| 版本历史 | 90天 | 每周 |

---

## 📦 项目脚本

### 根目录脚本 (package.json)

项目使用 **npm workspaces** 管理 frontend 和 backend 依赖。

```json
{
  "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
  "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
  "dev:frontend": "cd frontend && npm run dev",
  "dev:backend": "cd backend && npm run start:dev",
  "dev:ai": "cd ai-service && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
  "build": "npm run build:frontend && npm run build:backend",
  "build:frontend": "cd frontend && npm run build",
  "build:backend": "cd backend && npm run build",
  "docker:up": "docker-compose up -d",
  "docker:down": "docker-compose down",
  "docker:logs": "docker-compose logs -f",
  "docker:ps": "docker-compose ps",
  "clean": "rimraf node_modules frontend/node_modules backend/node_modules frontend/dist backend/dist",
  "lint": "npm run lint:frontend && npm run lint:backend",
  "lint:frontend": "cd frontend && npm run lint",
  "lint:backend": "cd backend && npm run lint"
}
```

### 后端脚本 (backend/package.json)

```json
{
  "build": "nest build",
  "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
  "start": "nest start",
  "start:dev": "nest start --watch",
  "start:debug": "nest start --debug --watch",
  "start:prod": "node dist/main",
  "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
  "test": "jest",
  "test:watch": "jest --watch",
  "test:cov": "jest --coverage",
  "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
  "test:e2e": "jest --config ./test/jest-e2e.json"
}
```

### 前端脚本 (frontend/package.json)

```json
{
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview"
}
```

---

## 🚀 开发命令

### 快速启动

```powershell
# 启动 Docker 服务
npm run docker:up

# 安装所有依赖
npm run install:all

# 启动后端（终端 1）
npm run dev:backend

# 启动前端（终端 2）
npm run dev:frontend

# 启动 AI 服务（终端 3）
npm run dev:ai
```

### 使用 concurrently 并行启动

```powershell
# 同时启动后端和前端
npm run dev
```

### 构建

```powershell
# 构建所有
npm run build

# 仅构建前端
npm run build:frontend

# 仅构建后端
npm run build:backend
```

### 测试

```powershell
# 后端测试
cd backend
npm run test

# 前端测试
cd frontend
npm run test

# E2E 测试
cd backend
npm run test:e2e
```

### 代码质量

```powershell
# Lint 所有代码
npm run lint

# 格式化代码
cd backend
npm run format
```

### Docker 操作

```powershell
# 启动 Docker 服务
npm run docker:up

# 停止 Docker 服务
npm run docker:down

# 查看日志
npm run docker:logs

# 查看服务状态
npm run docker:ps
```

### 清理

```powershell
# 清理所有 node_modules 和构建产物
npm run clean
```

---

## 🔧 环境配置

### 后端环境变量 (.env)

```env
# Application
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=audio_label

# Redis
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=

# JWT
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false
MINIO_BUCKET=audio-label

# AI Service
AI_SERVICE_URL=http://localhost:8000
```

### AI 服务环境变量 (.env)

```env
# Application
APP_NAME=Audio Label AI Service
DEBUG=True
PORT=8000

# Frontend
FRONTEND_URL=http://localhost:5173

# Redis
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=0

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=False
MINIO_BUCKET=audio-label

# AI Models
WHISPER_MODEL=base
WHISPER_DEVICE=cpu
PYANNOTE_MODEL=pyannote/speaker-diarization-3.1

# Processing
MAX_AUDIO_SIZE=524288000
SUPPORTED_FORMATS=["mp3", "wav", "flac", "ogg", "m4a"]
```

### 前端环境变量 (.env)

```env
# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api
VITE_WS_BASE_URL=ws://localhost:3000
VITE_AI_SERVICE_URL=http://localhost:8000
```

### Vite 代理配置

前端通过 Vite 开发服务器代理 API 和 WebSocket 请求：

```typescript
// vite.config.ts
server: {
  port: 5173,
  host: true,
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true,
    },
    '/ws': {
      target: 'ws://localhost:3000',
      ws: true,
    }
  }
}
```

---

## 📝 编码规范

### TypeScript 规范

- 使用 `type` 定义类型，`interface` 定义对象结构
- 避免使用 `any`，优先使用具体类型或 `unknown`
- 使用 `readonly` 标记不可变属性
- 使用 `enum` 或 `const assertion` 定义常量

### Vue 3 规范

- 使用 Composition API (`<script setup>`)
- 使用 TypeScript 进行类型定义
- 使用 `defineProps` 和 `defineEmits` 定义 props 和 events
- 使用 `ref` 和 `reactive` 定义响应式数据
- 使用 `computed` 定义计算属性
- 使用 `watch` 和 `watchEffect` 监听数据变化

### NestJS 规范

- 使用模块化架构（每个功能一个模块）
- 使用依赖注入（Constructor Injection）
- 使用装饰器（`@Controller`, `@Service`, `@Injectable`）
- 使用 DTO 进行数据验证
- 使用 `class-validator` 和 `class-transformer`
- 使用异常过滤器处理错误

### Python 规范

- 遵循 PEP 8 规范
- 使用类型注解（Type Hints）
- 使用 `pydantic` 进行数据验证
- 使用 `async/await` 处理异步操作
- 使用 FastAPI 的依赖注入系统

### Git 提交规范

使用 Conventional Commits 规范：

- `feat:` 新功能
- `fix:` Bug 修复
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 构建/工具相关

示例：
```
feat(auth): add JWT authentication
fix(audio): resolve audio player loading issue
docs(readme): update installation instructions
```

---

## 🧪 测试策略

### 测试类型

1. **单元测试**: 测试单个函数/组件
2. **集成测试**: 测试模块间交互
3. **E2E 测试**: 测试完整用户流程

### 测试框架

- 前端: Vitest / Jest
- 后端: Jest
- E2E: Playwright

### 测试命令

```powershell
# 前端测试
cd frontend
npm run test

# 后端测试
cd backend
npm run test

# 后端 E2E 测试
cd backend
npm run test:e2e
```

---

## 📊 性能优化

### 前端优化

- 使用虚拟滚动处理长列表
- 使用懒加载减少初始加载时间
- 使用 CDN 加速静态资源
- 使用 Gzip/Brotli 压缩
- 使用 Web Workers 处理重计算
- 使用 IndexedDB 本地缓存

### 后端优化

- 使用 Redis 缓存热点数据
- 使用数据库索引优化查询
- 使用连接池管理数据库连接
- 使用 Bull 队列处理异步任务
- 使用 Gzip/Brotli 压缩响应
- 使用 Nginx 负载均衡

### AI 服务优化

- 使用 GPU 加速模型推理
- 使用批量处理提高吞吐量
- 使用 Celery 异步任务队列
- 使用 Redis 缓存模型结果
- 使用分片处理长音频

---

## 🔐 安全方案

### 认证授权

- 使用 JWT 进行无状态认证
- 使用 Passport 集成多种认证策略
- 使用 RBAC 进行权限控制
- 使用 bcrypt 加密密码

### 数据安全

- 使用 HTTPS 加密传输
- 使用环境变量管理密钥
- 使用 CORS 限制跨域请求
- 使用输入验证防止注入攻击

### API 安全

- 使用速率限制防止滥用
- 使用 CORS 策略限制访问
- 使用 API Key 管理第三方访问

---

## 🚢 部署方案

### Docker 部署

```powershell
# 构建镜像
docker-compose build

# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f
```

### 生产环境配置

1. 设置 `NODE_ENV=production`
2. 使用生产数据库
3. 配置 HTTPS
4. 设置 CORS 白名单
5. 使用强 JWT 密钥
6. 配置 Nginx 反向代理

---

## 📚 文档资源

### 项目文档

- [README.md](./README.md) - 项目总览
- [QUICKSTART.md](./QUICKSTART.md) - 快速启动指南
- [需求文档](./docs/requirements.md) - 功能需求
- [技术方案](./docs/technical-specification.md) - 技术架构
- [设计系统](./docs/design-system.md) - 设计规范
- [项目结构](./docs/PROJECT_STRUCTURE.md) - 目录结构说明
- [开发计划](./docs/DEVELOPMENT_PLAN.md) - 开发规划

### 设计文档

- [设计原则](./docs/design-guidelines/01-principles.md)
- [交互规范](./docs/design-guidelines/02-interaction.md)
- [布局规范](./docs/design-guidelines/03-layout.md)
- [技术配置](./docs/design-guidelines/04-config.md)

### 原型文档

- [登录页原型](./docs/prototype/page-登录页.md)
- [首页原型](./docs/prototype/page-首页.md)
- [音频列表原型](./docs/prototype/page-音频列表.md)
- [标注界面原型](./docs/prototype/page-标注界面.md)
- [审核界面原型](./docs/prototype/page-审核界面.md)
- [任务列表原型](./docs/prototype/page-任务列表.md)

### HTML 原型

- [登录页](./docs/html-prototype/page-登录页.html)
- [首页](./docs/html-prototype/page-首页.html)
- [音频列表](./docs/html-prototype/page-音频列表.html)
- [标注界面](./docs/html-prototype/page-标注界面.html)
- [审核界面](./docs/html-prototype/page-审核界面.html)
- [任务列表](./docs/html-prototype/page-任务列表.html)

---

## 🔍 常见问题

### 开发环境问题

**Q: Docker 服务启动失败？**

A: 检查端口是否被占用：
```powershell
netstat -ano | findstr "5432"
netstat -ano | findstr "6380"
netstat -ano | findstr "9000"
```

**Q: 依赖安装失败？**

A: 尝试清理缓存后重新安装：
```powershell
npm run clean
npm run install:all
```

**Q: Python 依赖安装失败？**

A: 确保 Python 版本 >= 3.11，然后：
```powershell
cd ai-service
pip install --upgrade pip
pip install -r requirements.txt
```

### 开发流程问题

**Q: 如何添加新页面？**

A: 在 `frontend/src/pages/` 创建新的 Vue 组件，并在 `frontend/src/router/index.ts` 中配置路由。

**Q: 如何添加新 API？**

A: 
1. 后端：在 `backend/src/` 创建新的模块和控制器
2. 前端：在 `frontend/src/services/` 创建 API 服务

**Q: 如何修改数据库结构？**

A: 创建新的迁移文件：
```powershell
cd backend
npm run migration:generate -- --name=YourMigration
npm run migration:run
```

---

## 🎯 AI Agent 使用指南

### 上下文感知

当执行任务时，请始终考虑以下上下文：

1. **项目类型**: SaaS 平台，面向企业用户
2. **技术栈**: Vue 3 + NestJS + FastAPI
3. **设计风格**: Minimalism & Swiss Style
4. **核心功能**: 音频标注、AI 辅助、实时协作
5. **用户类型**: 标注员、审核员、管理员
6. **运行环境**: Windows (PowerShell)
7. **工作区**: 使用 npm workspaces 管理 frontend 和 backend
8. **端口配置**: Redis 使用 6380 端口（非默认 6379）

### 当前实现状态

#### 后端模块（已实现 ✅）
- `auth` - 认证模块（JWT、登录、注册）
- `users` - 用户模块（用户管理、个人资料）
- `projects` - 项目模块（项目管理）
- `teams` - 团队模块（团队管理、成员管理）
- `database` - 数据库模块（TypeORM 配置）

#### 后端模块（待实现 ⏳）
- `audio` - 音频模块（上传、下载、预览）
- `annotations` - 标注模块（标注管理、标签管理）
- `ai` - AI 模块（ASR、说话人分离、智能推荐）
- `collaboration` - 协作模块（实时协作、评论系统）
- `tasks` - 任务模块（任务分配、进度跟踪）
- `export` - 导出模块（多格式导出）
- `notifications` - 通知模块（通知推送）

#### 前端页面（已实现 ✅）
- `HomePage` - 首页
- `LoginPage` - 登录页
- `RegisterPage` - 注册页
- `AudioListPage` - 音频列表页
- `AnnotationPage` - 标注界面
- `ReviewPage` - 审核界面
- `TaskListPage` - 任务列表页

#### 前端目录结构（待完善 ⏳）
- `components` - 公共组件
- `composables` - 组合式函数
- `layouts` - 布局组件
- `utils` - 工具函数

### 开发规范

1. **遵循现有架构**: 不要随意修改核心架构
2. **保持一致性**: 遵循现有的代码风格和命名规范
3. **使用类型**: TypeScript 和 Python 都要使用类型注解
4. **添加测试**: 新功能必须包含测试
5. **更新文档**: 重要变更需要更新文档

### 决策原则

1. **优先用户体验**: 设计决策以用户需求为优先
2. **性能优先**: 优化加载时间和响应速度
3. **可维护性**: 代码要易于理解和维护
4. **可扩展性**: 考虑未来功能扩展
5. **安全性**: 始终考虑安全性问题

### 禁止事项

- ❌ 不要引入未经审核的第三方库
- ❌ 不要硬编码敏感信息
- ❌ 不要破坏现有功能
- ❌ 不要忽略测试
- ❌ 不要过度设计
- ❌ 不要复制粘贴代码而不理解

### 推荐工具

- **代码搜索**: 使用 `glob` 和 `search_file_content`
- **文件操作**: 使用 `read_file`、`write_file`、`replace`
- **命令执行**: 使用 `run_shell_command`（PowerShell）
- **项目管理**: 使用 `todo_write` 和 `todo_read`

### 开发优先级

基于当前实现状态，建议的开发优先级：

1. **高优先级**
   - 完成认证和用户管理功能
   - 实现音频上传和管理
   - 实现基础标注功能

2. **中优先级**
   - 实现 AI 辅助功能
   - 实现任务管理
   - 实现协作功能

3. **低优先级**
   - 实现导出功能
   - 实现通知系统
   - 优化性能和用户体验

---

## 📞 联系方式

如有问题或建议：
- 提交 [GitHub Issue](../../issues)
- 查看项目文档
- 联系团队

---

## 📄 许可证

MIT License

---

**最后更新**: 2026-02-14  
**文档版本**: v1.2.0